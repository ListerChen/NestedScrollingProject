<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.lister.nestedscrolltest"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.lister.nestedscrollingproject.simple.SimpleNestedActivity"></activity>
        <activity android:name="com.lister.nestedscrollingproject.mixed.MixedLayoutActivity" />
        <activity android:name="com.lister.nestedscrollingproject.coordinator.CoordinatorActivity" />
        <activity android:name="com.lister.nestedscrollingproject.bounce.BounceLayoutActivity" />
        <activity android:name="com.lister.nestedscrollingproject.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>